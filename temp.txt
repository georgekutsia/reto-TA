:root {
  color-scheme: dark light;
  font-family: Arial, Helvetica, sans-serif;
  --clr-black: #000000;
  --clr-charcoal: #111111;
  --clr-gray-dark: #333333;
  --clr-gray-muted: #3f3f3f;
  --clr-ink: #020617;
  --clr-night: #030712;
  --clr-midnight: #0f172a;
  --clr-navy-shadow: #041724;
  --clr-blue-hero: #0146a4;
  --clr-blue-link: #0d4ea4;
  --clr-blue-cta: #0363c4;
  --clr-blue-deep: #0047a3;
  --clr-blue-dark: #15567d;
  --clr-blue-sky: #4da3ff;
  --clr-cyan: #38bdf8;
  --clr-cyan-strong: #0ea5e9;
  --clr-void: #01060e;
  --clr-home-blue: #15567d;
  --clr-indigo: #1426c5;
  --clr-category-blue: #1f8bc7;
  --clr-teal: #00cc99;
  --clr-border-dark: #092333;
  --clr-silver: #ffffff;
  --clr-slate: #94a3b8;
  --clr-silver-dark: #999999;
  --clr-stone: #e2e8f0;
  --clr-slate-deep: #1f2937;
  --clr-white: #ffffff;
  --clr-red: #c51414;
  --clr-red-accent: #c51616;
  --clr-red-soft: #ff6a6a;
  --clr-red-bright: #f87171;
  --clr-red-primary: #ef4444;
  --clr-red-glow: #f34d4d;
  --clr-rose-light: #ffb3b3;
  --clr-green: #63c019;
  --clr-green-mid: #51d86b;
  --clr-green-deep: #26a235;
  --clr-green-dark: #21af3a;
  --clr-green-icon: #29b15f;
  --clr-green-glow: #8ef473;
  --clr-orange: #ff9900;
  --clr-amber: #f0b431;
  --clr-gold-deep: #996600;
  --clr-gold: #f5c800;
  --clr-gold-light: #ffdf52;
  --clr-gold-accent: #eab308;
  --clr-gold-bright: #f9db00;
  --clr-gold-shadow: #d1b800;
  --clr-magenta: #c800f2;
  --clr-pink: #a20051;
  --clr-burgundy: #2a0b0b;
  --clr-chocolate: #b45309;
  --clr-text-dark: #111111;
  --rgb-midnight: 15, 23, 42;
  --rgb-space: 2, 6, 23;
  --rgb-white: 255, 255, 255;
  --rgb-black: 0, 0, 0;
  --rgb-slate: 148, 163, 184;
  --rgb-sky: 56, 189, 248;
  --rgb-rose: 248, 113, 113;
  --rgb-warm-gray: 137, 137, 137;
  background-color: var(--clr-night);
  color: var(--clr-white);
  --surface: rgba(var(--rgb-midnight), 0.8);
  --surface-strong: rgba(var(--rgb-midnight), 0.9);
  --accent: var(--clr-cyan);
  --accent-strong: var(--clr-cyan-strong);
  --text-muted: var(--clr-slate);
  --border: rgba(var(--rgb-slate), 0.2);
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

button {
  position: relative;
  font-family: inherit;
}

button::after {
  content: "No funciona. Es solo visual para la prueba técnica...";
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  background: rgba(var(--rgb-black), 0.8);
  color: var(--clr-white);
  font-size: 20px;
  padding: 0.3rem 0.6rem;
  border-radius: 6px;
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.2s ease;
  z-index: 10;
}

button:hover::after {
  opacity: 1;
}

.os-header-actions button,
.os-footer a,
.premium-card a,
.os-premium button {
  position: relative;
  cursor: pointer;
}

.os-header-actions button::after,
.os-footer a::after,
.premium-card a::after,
.os-premium button::after {
  content: "No funciona. Es solo visual para la prueba técnica...";
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  background: rgba(var(--rgb-black), 0.8);
  color: var(--clr-white);
  font-size: 20px;
  padding: 0.3rem 0.6rem;
  border-radius: 6px;
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.2s ease;
  z-index: 10;
}

.os-header-actions button:hover::after,
.os-footer a:hover::after,
.premium-card a:hover::after,
.os-premium button:hover::after {
  opacity: 1;
}

.os-premium {
  position: relative;
  cursor: pointer;
}

.os-premium::after {
  content: "No funciona. Es solo visual para la prueba técnica...";
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  background: rgba(var(--rgb-black), 0.8);
  color: var(--clr-white);
  font-size: 20px;
  padding: 0.3rem 0.6rem;
  border-radius: 6px;
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.2s ease;
  z-index: 10;
}

.os-premium:hover::after {
  opacity: 1;
}

body {
  margin: 0;
  min-height: 100vh;
  background: radial-gradient(circle at 20% 20%, var(--clr-midnight) 0%, var(--clr-ink) 60%);
  color: var(--clr-white);
  font-family: Arial, Helvetica, sans-serif;
}

.workspace {
  --ws-pad: clamp(1.2rem, 4vw, 3rem);
  width: 100%;
  padding: 1.5rem calc(var(--ws-pad) + 260px) 2rem var(--ws-pad);
  position: relative;
}

.view-stack {
  width: 100%;
}

.os-page {
  background: var(--clr-white);
  color: var(--clr-charcoal);
  border: none;
  border-radius: 0;
  box-shadow: none;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  min-height: calc(100vh - 3rem);
}

.view.fullbleed .os-page {
  padding-top: 0;
}

.os-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.6rem 0;
  border-bottom: 3px solid var(--clr-silver);
  background: rgba(var(--rgb-white), 0.85);
  top: 0;
  z-index: 5;
}

.os-header .logo {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  font-size: 1.4rem;
  position: relative;
}

.logo-text {
  margin-left: 7rem;
}

.logo-icon {
  width: 6.5rem;
  height: 5rem;
  background: url("https://www.online-stopwatch.com/online-stopwatch.svg") no-repeat center;
  background-size: contain;
  display: inline-block;
  position: absolute;
  top: -10px;
}

.os-header-actions {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
  align-items: center;
}
.span-I{
  font-size: 1.5rem;
}

.os-header-actions .link {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  background: var(--clr-white);
  border: none;
  border-radius: 999px;
  padding: 0.35rem 0.9rem;
  font-weight: 600;
  font-size: .95rem;
  cursor: pointer;
}

.os-header-actions .link:hover span:last-child {
  text-decoration: underline;
}

.pill {
  border: 3px solid var(--clr-border-dark);
  border-radius: 999px;
  padding: 0.5rem 1.5rem;
  font-weight: 700;
  color: var(--clr-white);
  cursor: pointer;
}

.pill.green {
  background: var(--clr-green);
}

.pill.blue {
  background:  var(--clr-indigo);
}

.pill.red {
  background: var(--clr-red-accent);
}

.header-pill {
  font-size: 1rem;
}


.circle {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--clr-white);
}

.circle.green {
  background: var(--clr-green-icon);
}

.circle.gray {
  color: var(--clr-midnight);
}
.circle.gray i{
  color: var(--clr-gray-dark);
  border-radius: 50px;
  border: 2px solid var(--clr-gray-dark);
  font-size: 1.5rem;
  padding: 0.4rem;

}

.circle.white {
  background: var(--clr-white);
  color: var(--clr-gold-accent);
}

.circle.flag img {
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid var(--clr-black);
  padding: 2px;
}

.os-primary-nav {
  display: flex;
  flex-wrap: wrap;
  gap: 0.6rem;
  justify-content: center;
  padding: 0 4vw;
}

.nav-pill {
  --pill-color: var(--clr-home-blue);
  flex: 0 1 auto;
  border-radius: 999px;
  padding: 0.3rem 0.6rem;
  color: var(--clr-white);
  border: 3px solid var(--clr-border-dark);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: flex-start;
  gap: 0.35rem;
  text-align: left;
  background: var(--pill-color);
}

.nav-pill i {
  background: var(--color-white);
  width: 1.8rem;
  height: 1.8rem;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
  border: 2px solid rgba(var(--rgb-white), 0.85);
}
.nav-pill span{
  font-size: 1.2rem;
  
}
.red {
  --pill-color: var(--clr-red);
}
.green {
  --pill-color: var(--clr-green);
}
.purple {
  --pill-color: var(--clr-magenta);
}
.orange {
  --pill-color: var(--clr-orange);
}
.gray {
  --pill-color: var(--clr-silver-dark);
}
.pink {
  --pill-color: var(--clr-pink);
}
.blue {
  --pill-color: var(--clr-teal);
}
.navy {
  --pill-color: var(--clr-indigo);
}
.lime {
  --pill-color: var(--clr-green);
}
.magenta {
  --pill-color: var(--clr-magenta);
}
.amber {
  --pill-color: var(--clr-orange);
}

.os-banner {
  background: var(--clr-category-blue);
  color: var(--clr-white);
  padding: .9rem;
  text-align: center;
  font-size: 0.95rem;
  box-shadow: inset 0 0 12px rgba(var(--rgb-black), 0.25);
  border-top: 15px solid var(--clr-home-blue);
  border-bottom: 15px solid var(--clr-home-blue);
}
.os-banner div{
  background: var(--clr-home-blue);
  color: var(--clr-white);
  padding: .25rem;
  text-align: center;
  font-size: 0.95rem;
  box-shadow: inset 0 0 12px rgba(var(--rgb-black), 0.25);
  width: 50%;
  margin: auto;
  border-radius: 40px;
}
.os-banner div p{
  margin: auto;
  padding: 4px;
}


.os-cta-row {
  display: flex;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
}

.os-cta-row .pill {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  font-size: 1.3rem;
}

.os-category {
  padding: 1.5rem;
  background: var(--clr-category-blue);
  color: var(--clr-white);
  padding: .9rem;
  text-align: center;
  font-size: 0.95rem;
  box-shadow: inset 0 0 12px rgba(var(--rgb-black), 0.25);
  border-top: 15px solid var(--clr-home-blue);
  border-bottom: 15px solid var(--clr-home-blue);
}
.os-category span{
  font-size: 1.5rem;
}
.os-category i{
  border-radius: 50%;
  border: 2px solid white;
  padding: 4px;
  width: 3rem;
  height: 3rem;
}
.category-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  justify-content: center;
  padding: 0px 11vw;
}

.icon-btn {
  border: none;
  padding: 0.7rem 1.4rem;
  color: var(--clr-white);
  font-size: 1rem;
  cursor: pointer;
  min-width: 120px;

  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  border: 3px solid var(--clr-border-dark);
  background: var(--pill-color);

}

.icon-btn i,
.os-cta-row .pill i {
  color: var(--clr-white);
  font-size: 2.1rem;
}


.home {
  background: var(--clr-home-blue);
}

 .gold {
  background: var(--clr-gold-deep);
}

.tag-row {
  margin-top: 1rem;
  padding: 0.5rem 12vw;
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
  justify-content: center;
}

.tag-row button {
  background: var(--clr-blue-dark);
  color: var(--clr-white);
  font-size: 0.9rem;
  width: 150px;
  height: 30px;
  cursor: pointer;
}

.os-premium {
  text-align: center;
  background: linear-gradient(180deg, rgba(var(--rgb-white), 0.6), rgba(var(--rgb-white), 0.2));
}

.premium-card {
  position: relative;
  overflow: hidden;
  border: none;
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  background: linear-gradient(180deg, var(--clr-gold-light) 0%, var(--clr-gold-accent)) 100%;
  border-radius: 18px;
  font-weight: 900;
  box-shadow: 0 15px 25px rgba(var(--rgb-black), 0.2);
  color: var(--clr-midnight);
  width: 30%;
  min-width: 300px;
  height: 6rem;
  justify-content: center;
}

.premium-card p {
  font-size: 1.2rem;

}
.premium-card h3 {
  margin: 0;
  margin-top: -20px;
  font-size: 2.4rem;
}

.premium-card::after {
  content: "";
  position: absolute;
  top: 0;
  left: -80px;
  width: 60px;
  height: 100%;
  background: rgba(var(--rgb-white), 0.45);
  transform: skewX(20deg) translateX(-100%);
  animation: premiumShine 4s ease-in-out infinite;
}

.os-footer {
  text-align: center;
  font-size: 0.9rem;
  background: var(--clr-black);
  color: var(--clr-white);
  padding: 2.5rem 0;
}

.os-footer nav {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  justify-content: center;
}

.os-footer a {
  color: var(--clr-white);
  text-decoration: none;
  font-weight: 600;
  cursor: pointer;
}

.os-footer a:hover {
  text-decoration: underline;
}

.view-stack {
  min-height: calc(100vh - 5rem);
  position: relative;
}

.view {
  display: none;
  animation: fade 0.3s ease;
}

.view.is-active {
  display: block;
}

.view.fullbleed {
  margin-left: calc(-1 * var(--ws-pad));
  margin-right: calc(-1 * (var(--ws-pad) + 260px));
  margin-top: -2rem;
  margin-bottom: -2.5rem;
}

.view-header {
  margin-bottom: 1.5rem;
}

.eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.2em;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 0.5rem;
}

.view-header h1 {
  font-size: clamp(2rem, 4vw, 3rem);
  margin: 0 0 0.75rem;
}

.view-header p {
  color: var(--text-muted);
  max-width: 60ch;

}

.hero header h1 {
  font-size: clamp(2rem, 5vw, 3.5rem);
  margin-bottom: 0.2rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.hero header p {
  max-width: 60ch;
  color: var(--text-muted);
  margin-bottom: 1.5rem;
}

.stopwatch-card {
  background: linear-gradient(145deg, rgba(var(--rgb-midnight), 0.85), rgba(var(--rgb-midnight), 0.35));
  border: 1px solid var(--border);
  border-radius: 24px;
  padding: 2rem;
  position: relative;
  box-shadow: 0 20px 50px rgba(var(--rgb-space), 0.7);
  backdrop-filter: blur(18px);
}

.lcd-display {
  font-family: "Share Tech Mono", "Inter", monospace;
  font-size: clamp(2.8rem, 9vw, 5rem);
  display: flex;
  justify-content: center;
  align-items: baseline;
  padding: 1.5rem;
  background: var(--clr-black);
  border-radius: 18px;
  color: var(--clr-stone);
  border: 3px solid var(--clr-slate-deep);
  box-shadow: inset 0 0 20px rgba(var(--rgb-white), 0.05);
}

.lcd-display .time {
  min-width: 2ch;
  text-align: center;
}

.lcd-display .millis {
  font-size: 0.75em;
  min-width: 2ch;
}

.lcd-display .separator {
  margin: 0 0.25rem;
  opacity: 0.8;
}

.lcd-display .milli-separator {
  font-size: 0.8em;
  transform: translateY(-0.15em);
}

.controls {
  margin-top: 1.8rem;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 0.75rem;
}

.btn {
  border: 1px solid transparent;
  border-radius: 12px;
  padding: 0.95rem 1.5rem;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease,
    border-color 0.15s ease;
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

.btn:not(:disabled):hover {
  transform: translateY(-1px);
  box-shadow: 0 10px 20px rgba(var(--rgb-sky), 0.3);
}

.btn.primary {
  background: linear-gradient(120deg, var(--accent), var(--accent-strong));
  color: var(--clr-navy-shadow);
  border-color: transparent;
}

.btn.danger {
  background: linear-gradient(120deg, var(--clr-red-bright), var(--clr-red-primary));
  color: var(--clr-burgundy);
  border-color: transparent;
  box-shadow: 0 10px 25px rgba(var(--rgb-rose), 0.35);
}

.btn.secondary {
  border-color: var(--accent);
  color: var(--accent);
  background: transparent;
}

.btn.ghost {
  border-color: rgba(var(--rgb-white), 0.15);
  background: rgba(var(--rgb-white), 0.04);
  color: var(--clr-white);
}

.btn.small {
  padding: 0.5rem 0.8rem;
  font-size: 0.9rem;
}

.minimal-card {
  background: rgba(var(--rgb-midnight), 0.7);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 2rem;
  box-shadow: 0 10px 30px rgba(var(--rgb-space), 0.7);
}

.minimal-display {
  font-family: "Share Tech Mono", monospace;
  background: var(--clr-void);
  border-radius: 14px;
  border: 2px solid var(--clr-slate-deep);
  font-size: clamp(2.2rem, 6vw, 3.6rem);
  padding: 1.25rem;
  text-align: center;
  margin-bottom: 1.5rem;
}

.minimal-controls {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.minimal-card .note {
  margin-top: 1.5rem;
  color: var(--text-muted);
  line-height: 1.5;
}

.indicators {
  margin-top: 1.5rem;
  display: flex;
  justify-content: flex-end;
}

.toggle-sound {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  cursor: pointer;
  font-size: 0.95rem;
  color: var(--text-muted);
}

.toggle-sound input {
  display: none;
}

.toggle-sound .slider {
  width: 46px;
  height: 24px;
  border-radius: 999px;
  background: rgba(var(--rgb-slate), 0.4);
  position: relative;
  transition: background 0.2s ease;
}

.toggle-sound .slider::after {
  content: "";
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--clr-white);
  position: absolute;
  top: 50%;
  left: 4px;
  transform: translateY(-50%);
  transition: transform 0.2s ease, background 0.2s ease;
}

.toggle-sound input:checked + .slider {
  background: var(--accent);
}

.toggle-sound input:checked + .slider::after {
  transform: translate(18px, -50%);
  background: var(--clr-midnight);
}

.laps-section {
  background: var(--surface);
  border-radius: 20px;
  border: 1px solid var(--border);
  padding: 1.5rem;
  backdrop-filter: blur(16px);
}

.laps-section header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1rem;
}

.laps-list {
  list-style: none;
  margin: 0;
  padding: 0;
  max-height: 320px;
  overflow-y: auto;
}

.laps-list li {
  display: grid;
  grid-template-columns: 100px auto 120px;
  padding: 0.75rem 0;
  border-bottom: 1px solid rgba(var(--rgb-slate), 0.18);
  font-family: "Share Tech Mono", "Inter", monospace;
  align-items: baseline;
  gap: 1rem;
}

.laps-list li span {
  color: var(--text-muted);
}

.laps-list li strong {
  color: var(--clr-white);
  font-weight: 600;
}

.laps-list li:last-child {
  border-bottom: none;
}

.roadmap-card {
  background: var(--surface-strong);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 1.75rem;
  line-height: 1.6;
  box-shadow: 0 12px 35px rgba(var(--rgb-space), 0.65);
}

.roadmap-card ul {
  margin: 0 0 1.2rem 1rem;
  padding: 0;
  color: var(--text-muted);
}

.roadmap-card li {
  margin-bottom: 0.4rem;
}

.view-switcher {
  background: rgba(var(--rgb-midnight), 0.7);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  box-shadow: 0 12px 30px rgba(var(--rgb-space), 0.6);
  position: fixed;
  right: clamp(1rem, 3vw, 2.5rem);
  top: 50%;
  transform: translateY(-50%);
  width: 220px;
  z-index: 10;
}

.view-switcher p {
  text-transform: uppercase;
  letter-spacing: 0.16em;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin: 0 0 0.25rem;
}

.view-switcher button {
  border-radius: 12px;
  padding: 0.8rem 1rem;
  border: 1px solid rgba(var(--rgb-slate), 0.3);
  background: rgba(var(--rgb-midnight), 0.4);
  color: var(--clr-stone);
  cursor: pointer;
  font-size: 0.95rem;
  font-weight: 600;
  transition: background 0.2s ease, border-color 0.2s ease;
}

.view-switcher button.active {
  border-color: var(--accent);
  background: rgba(var(--rgb-sky), 0.15);
  color: var(--accent);
}

.view-switcher button:hover {
  border-color: var(--accent);
}

.view-switcher button[data-view-target="intento-1"]:hover::after {
  content: "primera generación de contador básico";
  display: block;
  font-size: 0.75rem;
  color: var(--clr-white);
  margin-top: 0.35rem;
  text-transform: none;
  letter-spacing: normal;
}

.view-switcher__extras {
  display: flex;
  gap: 0.5rem;
}

.view-switcher__extras button {
  background: var(--clr-indigo);
  color: var(--clr-white);
  border: none;
  padding: 0.6rem 1rem;
  border-radius: 999px;
  font-weight: 600;
  cursor: pointer;
  flex: 1;
}

.view-switcher__extras button:hover {
  background: var(--clr-blue-cta);
}

.audio-popup {
  position: fixed;
  right: clamp(1rem, 3vw, 2.5rem);
  bottom: 2rem;
  width: min(320px, 90vw);
  background: rgba(var(--rgb-midnight), 0.92);
  border: 1px solid rgba(var(--rgb-slate), 0.4);
  border-radius: 16px;
  padding: 1.25rem;
  box-shadow: 0 12px 24px rgba(var(--rgb-space), 0.5);
  opacity: 0;
  pointer-events: none;
  transform: translateY(12px);
  transition: opacity 0.25s ease, transform 0.25s ease;
  z-index: 12;
}

.audio-popup.is-visible {
  opacity: 1;
  pointer-events: auto;
  transform: translateY(0);
}

.audio-popup__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 1rem;
  margin-bottom: 0.5rem;
}

.audio-popup__header h4 {
  margin: 0;
}

.audio-popup__hint {
  font-size: 0.85rem;
  color: var(--clr-slate);
  margin: 0 0 0.75rem;
}

.audio-popup__controls {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  flex-wrap: wrap;
}

.audio-popup__controls button {
  border: none;
  border-radius: 999px;
  padding: 0.45rem 1.2rem;
  font-weight: 600;
  cursor: pointer;
  background: var(--clr-indigo);
  color: var(--clr-white);
}

.audio-popup__controls button[data-action="stop"] {
  background: var(--clr-red-primary);
}

.audio-popup__controls label {
  display: flex;
  flex-direction: column;
  font-size: 0.85rem;
  color: var(--clr-stone);
}

.audio-popup__controls select {
  margin-top: 0.15rem;
  border-radius: 8px;
  border: 1px solid rgba(var(--rgb-slate), 0.4);
  background: rgba(var(--rgb-midnight), 0.6);
  color: var(--clr-stone);
  padding: 0.25rem 0.5rem;
}

.audio-popup__close {
  background: transparent;
  border: none;
  color: var(--clr-white);
  font-size: 1.1rem;
  cursor: pointer;
}

.audio-popup audio {
  width: 100%;
  margin-bottom: 0.75rem;
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
}

@keyframes fade {
  from {
    opacity: 0;
    transform: translateY(6px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 640px) {
  .workspace {
    --ws-pad: 1rem;
    padding: 1rem;
  }

  .view.fullbleed {
    margin: 0 calc(-1 * var(--ws-pad));
  }

  .view-switcher {
    position: static;
    transform: none;
    width: 100%;
    margin-top: 1rem;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .view-switcher button {
    flex: 1 1 150px;
    text-align: center;
  }

  .lcd-display {
    flex-wrap: wrap;
    font-size: 2.5rem;
  }

  .controls {
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  }

  .laps-list li {
    grid-template-columns: 70px auto;
    grid-template-areas:
      "lap total"
      ". diff";
  }

  .laps-list li span {
    grid-area: diff;
  }

  .laps-list li strong:first-child {
    grid-area: lap;
  }

  .laps-list li strong:last-child {
    grid-area: total;
  }
}

i{
  color: white;
  font-size: 20px;
}
.premiumShow {
  position: relative;
  overflow: hidden;
  border: 0;
  border-radius: 12px;
  padding: 0.5rem 1.5rem 0.5rem 0.5rem;
  background: linear-gradient(180deg, var(--clr-gold-bright) 0%, var(--clr-gold-shadow) 100%);
  color: var(--clr-midnight);
  font-weight: 700;
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  box-shadow: 0 7px 4px -2px rgba(var(--rgb-warm-gray), 0.5);
  cursor: pointer;
}

.premiumShow .prem {
  position: absolute;
  top: 0;
  left: -70px;
  width: 50px;
  height: 100%;
  background: rgba(var(--rgb-white), 0.5);
  transform: skewX(20deg) translateX(-100%);
  animation: premiumShine 4s ease-in-out infinite;
}

.premiumShow .premium-text {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  text-decoration: none;
}

.premiumShow .premium-text,
.premiumShow .circle.green i {
  transition: text-decoration 0.2s ease;
}

.premiumShow .premium-text i {
  color: var(--clr-chocolate);
}

.premiumShow:hover .premium-text {
  text-decoration: underline;
}

@keyframes premiumShine {
  0%, 95%{
    transform: skewX(20deg) translateX(-120%);
  }

  45% {
    transform: skewX(20deg) translateX(720%);
  }
}
